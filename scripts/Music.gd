extends AudioStreamPlayer

func _ready():
#	stream = load("res://sound/music/city_track0.ogg")
#	playing = true
#	pause_mode = Node.PAUSE_MODE_PROCESS
	pass
#
#func _process(delta):
#	if Input.is_action_just_pressed("mute") and !stream_paused:
#		stream_paused = true
#	elif Input.is_action_just_pressed("mute") and stream_paused:
#		stream_paused = false
